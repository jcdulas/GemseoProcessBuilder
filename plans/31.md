# 31 — Image export and report

Status: Not started
Depends on: 21, 24, 25
SPEC: § 13

## Goal

Export any view as SVG or PNG, and generate a project report as standalone HTML and PDF.

## Scope

In:
- **Image export** (`File › Export › Image…` and a context-menu entry on each view): canvas (current level, or whole model expanded), N2, XDSM, every results chart.
  - SVG: serialize the view's SVG with computed styles inlined (only the properties used by our CSS), fonts as system fallbacks, `viewBox` fitted to content, white background.
  - PNG: render the standalone SVG in an offscreen `QWebEnginePage` (or `QSvgRenderer` if the SVG uses only supported features) at 1×, 2× or 4×.
  - Bridge method `image.export(svg, format, scale, path)`.
- **Report** (`File › Export › Report…`): options dialog to select the content of SPEC § 13 (project description, diagrams, component and variable inventory, problem definition, selected runs, selected post-processing images); output:
  - standalone HTML: one file, inline SVG, base64 images, print-friendly CSS, table of contents;
  - PDF: the same HTML printed with `QWebEnginePage.printToPdf` (A4, margins, page breaks before sections).
  - Report text in English, generated from the project data (no free-text AI generation).

Out:
- XDSM-specific exports (done in plan 25).

## Deliverables

```
static/js/lib/svg_export.js               # pure-ish: style inlining given a style map, viewBox fitting
static/js/services/export.js              # collects SVG from views
gemseo_process_builder/app/image_export.py
gemseo_process_builder/report/builder.py  # ReportOptions → HTML string
gemseo_process_builder/report/templates/  # HTML skeleton and CSS (string templates, no Jinja dependency)
gemseo_process_builder/report/pdf.py
tests/js/svg_export.test.js
tests/python/report/test_builder.py
```

## Steps

- [ ] SVG export library and tests (style inlining on a fake DOM-free structure).
- [ ] View integrations and export dialog.
- [ ] PNG rendering.
- [ ] Report builder with tests on the Sellar example (structure, sections present, images embedded).
- [ ] PDF printing and options dialog.

## Tests

- `svg_export.test.js`: viewBox from bounding boxes, style map application, background insertion.
- Report builder: selected sections only, inventory tables content, base64 images present, HTML is standalone (no external URLs).

## Acceptance criteria

- The exported SVG of the Sellar canvas opens correctly in a browser and in Inkscape; PNG at 2× is sharp.
- The report of the Sellar MDF project opens offline and prints to a clean PDF.
- `python tools/check.py` passes.

## Implementation notes

_To be filled when the plan is done._
