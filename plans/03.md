# 03 — HTML application shell and preferences

Status: Not started
Depends on: 02
SPEC: § 8.1, § 8.8, § 8.9, § 1.2 (English, light theme)

## Goal

Build the empty but complete application frame: panels, tabs, toolbar, status bar, native menus wired to JS actions, keyboard shortcuts, and persistent preferences. Later plans only fill panels.

## Scope

In:
- Layout of SPEC § 8.1: left panel (Library / Tree tabs), center tabbed area (Workflow tab fixed, other tabs closable), right Inspector, bottom panel (Console / Problems / Runs tabs), toolbar, status bar.
- Resizable splitters, collapsible panels, layout persisted in preferences.
- Action registry shared by native menus, toolbar buttons and shortcuts.
- Preferences storage (Python) and `prefs.get` / `prefs.set`.
- Console panel showing application logs (Python `logging` → events).
- Design tokens (CSS custom properties), light theme inspired by ModelCenter.

Out:
- Panel contents other than Console (later plans).
- Undo/redo behavior (plan 05), project actions behavior (plan 04): the actions exist but are disabled.

## Deliverables

```
gemseo_process_builder/app/preferences.py     # Preferences (Pydantic) + JSON file in the user config dir
gemseo_process_builder/app/api_prefs.py       # prefs.get / prefs.set
gemseo_process_builder/app/actions.py         # native menu definitions → "action.invoke" events
gemseo_process_builder/app/log_forwarding.py  # logging.Handler → "app.log" events (rate limited)
static/index.html                             # full layout skeleton
static/css/tokens.css, layout.css, components.css
static/js/shell/layout.js                     # splitters, collapse, tabs, persistence
static/js/shell/actions.js                    # action registry: id, label, shortcut, enabled, run()
static/js/shell/shortcuts.js
static/js/shell/statusbar.js
static/js/panels/console.js
static/js/lib/splitter.js                     # pure size computations
static/js/lib/log_buffer.js                   # pure bounded ring buffer + filtering
tests/python/test_preferences.py
tests/js/splitter.test.js, log_buffer.test.js
```

## Design

- Preferences file: `QStandardPaths.AppConfigLocation/preferences.json`, schema-versioned Pydantic model: `catalog_paths`, `python_interpreter`, `max_undo` (500), `layout` (panel sizes and collapsed states), `recent_projects`, `log_level`, `stop_timeout_s` (10), `allow_concurrent_runs` (false). Invalid file → backup and defaults, with a warning in the console.
- Actions have ids like `file.open`, `edit.undo`, `run.start`. Native menus are defined in Python from a static list that mirrors the JS registry (id, label, shortcut). Clicking a native menu item emits `action.invoke { id }`; JS runs the action. JS sends `actions.state { id: enabled }` updates to keep native menus in sync (`actions.setState` method).
- Shortcuts are handled in JS (the web view has focus), except when a text input is focused. Native menus show the shortcut text but do not register it, to avoid double triggering.
- Console: bounded buffer (10,000 lines), level filter, text search, copy, clear. Each line: time, level, source (`app`, `worker`, `run:<id>`), message.
- Visual: system UI font, 13 px base; neutral grays, one accent blue; component type colors defined as tokens for later plans.

## Steps

- [ ] Preferences model, load/save, API methods.
- [ ] Layout HTML/CSS with the panels and tabs of § 8.1; placeholders say "Empty — implemented in plan NN".
- [ ] Splitters and collapse; restore sizes from preferences at startup; save on change (debounced 500 ms).
- [ ] Action registry, toolbar, native menus, shortcuts list of § 8.9 (actions disabled until their plan).
- [ ] Logging forwarding and Console panel.
- [ ] Status bar areas: project, modified flag, worker status, run progress (placeholders).

## Tests

- `test_preferences.py`: defaults, round trip, corrupted file fallback, unknown keys ignored.
- `splitter.test.js`: min/max sizes, collapse/expand restores previous size.
- `log_buffer.test.js`: capacity, filter by level, search.

## Acceptance criteria

- The window looks like the § 8.1 sketch; panels resize and collapse; the layout survives a restart.
- `File › Quit` and `View` toggles work from the native menu; shortcuts do not fire while typing in an input.
- Python log messages appear in the Console.
- `python tools/check.py` passes.

## Implementation notes

_To be filled when the plan is done._
